# Group blocks of code
# There is need for code that is clean, reusable and maintainable
# functions
# importing the afternoon module in our file
import afternoon_module as m


def afternoon():
    print("Class starts at 2pm")


# calling a function
afternoon()

# arguments and parameters
# arguments are specified after the function
# parameters are

# python modules
# we need modules to group our code for readability
# they can be imported to the python file where we need to use them
print(m.product(2, 3))
